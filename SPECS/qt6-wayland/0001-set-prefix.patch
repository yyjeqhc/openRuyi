diff --git a/cmake/QtWaylandSetup.cmake b/cmake/QtWaylandSetup.cmake
index 56e3c12..daa35df 100644
--- a/cmake/QtWaylandSetup.cmake
+++ b/cmake/QtWaylandSetup.cmake
@@ -6,8 +6,14 @@ function(qt_internal_get_wayland_protocols_dir out_var)
         set(qtbase_wayland_protocols_dir
             "${QT_BUILD_DIR}/${INSTALL_SHAREDIR}/qt6/wayland/protocols/")
     else()
+        # Fallback to /usr if QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX is empty or just "/"
+        if(NOT QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX OR QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX STREQUAL "/")
+            set(prefix "/usr")
+        else()
+            set(prefix "${QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX}")
+        endif()
         set(qtbase_wayland_protocols_dir
-            "${QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX}/${QT6_INSTALL_SHAREDIR}/qt6/wayland/protocols/")
+            "${prefix}/${QT6_INSTALL_SHAREDIR}/qt6/wayland/protocols/")
     endif()
     set(${out_var} "${qtbase_wayland_protocols_dir}" PARENT_SCOPE)
 endfunction()
@@ -17,8 +23,14 @@ function(qt_internal_get_wayland_extensions_dir out_var)
         set(qtbase_wayland_extensions_dir
             "${QT_BUILD_DIR}/${INSTALL_SHAREDIR}/qt6/wayland/extensions/")
     else()
+        # Fallback to /usr if QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX is empty or just "/"
+        if(NOT QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX OR QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX STREQUAL "/")
+            set(prefix "/usr")
+        else()
+            set(prefix "${QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX}")
+        endif()
         set(qtbase_wayland_extensions_dir
-            "${QT_BUILD_INTERNALS_RELOCATABLE_INSTALL_PREFIX}/${QT6_INSTALL_SHAREDIR}/qt6/wayland/extensions/")
+            "${prefix}/${QT6_INSTALL_SHAREDIR}/qt6/wayland/extensions/")
     endif()
     set(${out_var} "${qtbase_wayland_extensions_dir}" PARENT_SCOPE)
 endfunction()
