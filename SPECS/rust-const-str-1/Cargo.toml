# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.77.0"
name = "const-str"
version = "1.1.0"
authors = ["Nugine <nugine@foxmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "compile-time string operations"
readme = "README.md"
keywords = [
    "string",
    "const",
    "proc-macro",
]
categories = [
    "text-processing",
    "no-std",
]
license = "MIT"
repository = "https://github.com/Nugine/const-str"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[features]
all = [
    "std",
    "proc",
    "regex",
    "http",
    "case",
]
case = [
    "proc",
    "const-str-proc-macro?/heck",
    "dep:heck",
]
default = []
http = [
    "proc",
    "const-str-proc-macro?/http",
    "dep:http",
]
proc = ["dep:const-str-proc-macro"]
regex = [
    "proc",
    "const-str-proc-macro?/regex",
    "dep:regex",
]
std = []
unstable = []

[lib]
name = "const_str"
path = "src/lib.rs"

[dependencies.const-str-proc-macro]
version = "1.1.0"
optional = true

[dependencies.heck]
version = "0.5.0"
optional = true

[dependencies.http]
version = "1.4.0"
optional = true

[dependencies.regex]
version = "1.12.2"
optional = true
